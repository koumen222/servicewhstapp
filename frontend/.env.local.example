# Backend API URL
# Backend API base URL (no trailing slash, no /api suffix — the client adds paths directly)
NEXT_PUBLIC_API_URL=http://localhost:3001

# App name displayed in UI
NEXT_PUBLIC_APP_NAME=WhatsApp SaaS

# App public URL (used for payment redirects)
NEXT_PUBLIC_APP_URL=http://localhost:3000
